{% extends 'layout.twig' %}

{% block body %}
    <div class="row">
        {% for color in card_colors %}
            <div class="col-sm-6 col-md-4 col-lg-3 mb-4">
                <div class="card-profile">
                    <div class="card-profile-header card-profile-header-{{ color }}">

                    </div>
                    <div class="card-picture">
                        <span class="image--cover image--cover--{{ color }}">
                            <i class="fas fa-user fa-4x" aria-hidden="true"></i>
                        </span>
                    </div>
                    <div class="card-profile-body card-profile-body-{{ color }}">
                            <span>Coudenne Nicolas</span>
                            <span>Développeur</span>
                        <span class="separator">
                        </span>
                        <span>
                        <a href="#" class="btn">
                            Message
                        </a>
                        </span>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock %}